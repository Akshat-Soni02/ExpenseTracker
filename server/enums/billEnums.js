export const BillStatus = Object.freeze({
    PENDING: "pending",
    MISSED: "missed",
    PAID: "paid",
});

export const BillMemberStatus = Object.freeze({
    PENDING: "pending",
    PAID: "paid",
});